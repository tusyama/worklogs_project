/** React Hook Form / API field names for work entries. */
export const ENTRY_FORM_FIELD = {
  completedAt: "completedAt",
  workTypeId: "workTypeId",
  volume: "volume",
  unit: "unit",
  performerName: "performerName",
} as const;
